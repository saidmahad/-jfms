# JUPA Fuel Station Management System — Frontend environment
# Copy to .env.local for local use. All variables are optional.

# Absolute URL of the deployed backend API (no trailing slash).
# Example: https://jfms-api.onrender.com
# When unset, the frontend calls /api (Vite dev proxy to http://localhost:4000).
VITE_API_URL=
